{
  "name": "utils",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "uploadFiles.cjs",
  "scripts": {
    "before": "unset AWS_SECRET_ACCESS_KEY && unset AWS_ACCESS_KEY_ID",
    "fetch": "npm run fetch:southamerica && npm run fetch:northamerica && npm run fetch:africa && npm run fetch:europe && npm run fetch:asia && npm run fetch:oceania",
    "upload": "npm run before && npm run upload:southamerica && npm run upload:northamerica && npm run upload:africa && npm run upload:europe && npm run upload:asia && npm run upload:oceania",
    "fetch:southamerica": "AWS_REGION=sa-east-1 AWS_BUCKET_BASE_NAME=latency-test-pfg-unicamp node fetchFiles.js",
    "fetch:northamerica": "AWS_REGION=us-east-1 AWS_BUCKET_BASE_NAME=latency-test-pfg-unicamp node fetchFiles.js",
    "fetch:africa": "AWS_REGION=af-south-1 AWS_BUCKET_BASE_NAME=latency-test-pfg-unicamp node fetchFiles.js",
    "fetch:europe": "AWS_REGION=ap-northeast-1 AWS_BUCKET_BASE_NAME=latency-test-pfg-unicamp node fetchFiles.js",
    "fetch:asia": "AWS_REGION=eu-west-1 AWS_BUCKET_BASE_NAME=latency-test-pfg-unicamp node fetchFiles.js",
    "fetch:oceania": "AWS_REGION=ap-southeast-1 AWS_BUCKET_BASE_NAME=latency-test-pfg-unicamp node fetchFiles.js",
    "upload:southamerica": "AWS_PROFILE=unicamp AWS_REGION=sa-east-1 AWS_BUCKET_BASE_NAME=latency-test-pfg-unicamp node uploadFiles.cjs",
    "upload:northamerica": "AWS_PROFILE=unicamp AWS_REGION=us-east-1 AWS_BUCKET_BASE_NAME=latency-test-pfg-unicamp node uploadFiles.cjs",
    "upload:africa": "AWS_PROFILE=unicamp AWS_REGION=af-south-1 AWS_BUCKET_BASE_NAME=latency-test-pfg-unicamp node uploadFiles.cjs",
    "upload:asia": "AWS_PROFILE=unicamp AWS_REGION=ap-northeast-1 AWS_BUCKET_BASE_NAME=latency-test-pfg-unicamp node uploadFiles.cjs",
    "upload:europe": "AWS_PROFILE=unicamp AWS_REGION=eu-west-1 AWS_BUCKET_BASE_NAME=latency-test-pfg-unicamp node uploadFiles.cjs",
    "upload:oceania": "AWS_PROFILE=unicamp AWS_REGION=ap-southeast-1 AWS_BUCKET_BASE_NAME=latency-test-pfg-unicamp node uploadFiles.cjs"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "async-mutex": "^0.3.2",
    "aws-sdk": "^2.1125.0",
    "axios": "^0.27.2"
  }
}
